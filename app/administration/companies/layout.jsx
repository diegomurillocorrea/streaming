export const metadata = {
    title: "Companies | Streaming Murillo App",
};

export default function CompaniesLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
