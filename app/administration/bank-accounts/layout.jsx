export const metadata = {
    title: "Bank Accounts | Streaming Murillo App",
};

export default function BankAccountsLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
