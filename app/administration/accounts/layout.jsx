export const metadata = {
    title: "Accounts | Streaming Murillo App",
};

export default function AccountsLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
